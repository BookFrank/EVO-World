management:
  server:
    port: 7002
  endpoints:
    enabled-by-default: true

#  endpoints:
#    web:
#      exposure:
#        include: *
  endpoint:
    health:
      show-details: always



evo:
  module: springboot
  value: ${random.value}
  number: ${random.int}
  bignumber: ${random.long}
  test1: ${random.int(10)}
  test2: ${random.int[10,20]}

tazine:
  est: 2016